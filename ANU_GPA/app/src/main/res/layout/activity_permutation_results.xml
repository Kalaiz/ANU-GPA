<?xml version="1.0" encoding="utf-8"?>

<!--Authorship Kalai, u6555407-->

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/outerScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PermutationResults">

    <TableLayout
        android:id="@+id/headingTableLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/headingTableLayout"
        ></TableLayout>

    <ScrollView
        android:id="@+id/innerScrollView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/headingTableLayout">
    <TableLayout
        android:id="@+id/possibleResultsTableLayout"
        android:layout_width="wrap_content"
        android:layout_height="600dp"
        >
    </TableLayout>
    </ScrollView>

</RelativeLayout>